le(c,a).
le(a,b).
le(c,d).


w_zbiorze(X) :-
	le(X, _);
	le(_, X).
	

zwrotny() :- 
	le(X,X).


przechodni() :-
	\+(\+le(X,Y); \+le(Y,X); X \= Y).

antysym() :-
	\+(\+le(X,Y); \+le(Y,Z); \+le(X,Z)).

porzadek() :- 
	zwrotny(),
	przechodni(),
	antysym().

